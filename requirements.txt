# App dependencies
flask
requests
pandas
kagglehub
python-dotenv
cryptography
bcrypt
pytest
pytest-cov
ruff
gunicorn
prometheus-client

# Note: sqlite3 is part of Python's standard library and
# should not be installed via pip.
